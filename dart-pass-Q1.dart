main()
{
  var hour=DateTime.now().hour;
  print(hour);
  /*The error in the program is the word const because it is used for static
   variables whose value never changes.
   To solve this problem, the word const is replaced with the word var  */
}